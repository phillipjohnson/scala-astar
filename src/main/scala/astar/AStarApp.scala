package astar

import scala.scalajs.js.JSApp
import org.scalajs.dom
import dom.document

import scala.scalajs.js.annotation.JSExport

object AStarApp extends JSApp {
  def main(): Unit = {
    //val mazeNode = document.createTextNode(Mazes.maze1)
    //document.body.appendChild(mazeNode)
  }

}